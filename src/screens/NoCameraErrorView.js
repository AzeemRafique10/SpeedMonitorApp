import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const NoCameraErrorView = () => {
  return (
    <View>
      <Text>NoCameraErrorView</Text>
    </View>
  );
};

export default NoCameraErrorView;

const styles = StyleSheet.create({});
